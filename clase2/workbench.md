# MySQL Workbench

> 💡 MySQL Workbench es el administrador de servidores de base de datos oficial para trabajar con MySQL
> Como el servidor de base de datos no tiene interfaz gráfica amigable, por eso necesitamos esta herramienta
> una vez que lo agregamos primero vamos a necesitar conectarnos al servidor de base de datos

> vamos a escribir código SQL en la ventana código
> Y luego cuando lo ejecutemos venimos el resultado en la ventana 'Output'

## Activar una base de datos

> Para poder interactuar con una base de datos debemos activarla primero
> el comando para activar una base de datos es:

    USE nombreBase;

> Para ejecutar los comandos tenemos un icono y también un atajo de teclado
1. el icono es el "rayito" ⚡ que está justo arriba de la paleta de código
2. El atajo de teclado es CTRL + INTRO (1 comando)
3. El atajo de teclado es CTRL + SHIFT + INTRO (más comando)